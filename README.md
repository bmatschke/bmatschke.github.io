# Academic webpage

Static site files for an academic webpage (plain HTML + CSS).

## Publish on GitHub Pages

- **User site (recommended):** create a repo named `bmatschke.github.io`, push this content to its default branch, and GitHub will serve it at `https://bmatschke.github.io/`.
- **Project site:** in GitHub, go to **Settings → Pages**, choose the default branch and the `/ (root)` folder.

